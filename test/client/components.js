// const {users} = require('../../io/database/seeds/static_data')
// const {
//   expect,
//   mount,
//   shallow,
//   React,
//   App,
//   ListCoaches
// } = require('../setup')
// require('isomorphic-fetch')
//
// describe('Testing components', () => {
//   // describe('<App /> render', () => {
//   //   it('checks if elements in App component render', () => {
//   //     const APP_URL = 'http://localhost:3000'
//   //     const wrapper = mount(<App />)
//   //     expect(wrapper.find('ul')).to.have.length(1)
//   //     expect(wrapper.find('li')).to.have.length(5)
//   //   })
//   // })
//   describe('<ListCoaches /> render', () => {
//     it('tests setting props', () => {
//       const wrapper = mount(<ListCoaches list={users} />)
//       expect(wrapper.props().list).to.be.defined
//       expect(wrapper.props().list).to.eql(users)
//       expect(wrapper.find('ul')).to.have.length(1)
//       expect(wrapper.find('li')).to.have.length(5)
//     })
//   })
// })
