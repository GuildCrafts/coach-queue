//functions that work with user data
//example: find which day of the week has the most appointments

const findFirstAppointment = (activeCoaches, appointmentLengthMin) => {
  
}

module.exports = {
  findFirstAppointment
}